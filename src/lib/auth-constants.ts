/**
 * Shared auth constants used across client + server.
 *
 * Keep this file free of "use client" so it can be imported from `proxy.ts`
 * and route handlers.
 */

export const FIREBASE_AUTH_COOKIE = "firebaseAuth" as const;


